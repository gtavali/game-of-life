server:
  port: 8090

spring:
  h2:
    console:
      enabled: true
  datasource:
    url: jdbc:h2:mem:gameoflife
    driverClassName: org.h2.Driver
    username: db
    password: db
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect